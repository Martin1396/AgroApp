# Producción: el navegador llama /api en el MISMO dominio del frontend.
# Vercel reescribe /api → backend (ver scripts/sync-vercel-proxy.mjs).
VITE_API_URL=/api
VITE_API_BACKEND_URL=https://agro-app-nine.vercel.app
