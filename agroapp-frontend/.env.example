# Desarrollo (npm run dev): proxy Vite → localhost:3001
VITE_API_URL=/api

# Solo para build en Vercel (no se embebe en el bundle del navegador):
# URL base del backend SIN /api al final
# VITE_API_BACKEND_URL=https://agro-app-nine.vercel.app
